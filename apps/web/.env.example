# ============================================
# Cấu hình Supabase
# ============================================
# Lấy từ: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# Cấu hình Cloudinary
# ============================================
# Lấy từ: https://console.cloudinary.com/console
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Upload preset cho uploads không cần chữ ký (tạo trong Cloudinary dashboard)
# Settings > Upload > Upload presets > Add upload preset
# Đặt "Signing Mode" thành "Unsigned"
NEXT_PUBLIC_CLOUDINARY_PRESET_NAME=your-preset-name

# ============================================
# Cấu hình Next.js
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Admin Route (Tùy chọn - Bảo mật bổ sung)
# ============================================
# Đường dẫn tùy chỉnh cho admin dashboard (mặc định: /admin)
# Production: Nên dùng đường dẫn khó đoán như /admin-xyz-secret-2024
# QUAN TRỌNG: Đây KHÔNG thay thế authentication, chỉ là lớp bảo vệ bổ sung
# NEXT_PUBLIC_ADMIN_PATH=/admin

# ============================================
# Xác thực (Tùy chọn - cho production)
# ============================================
# Supabase Auth JWT Secret (lấy từ Supabase dashboard)
# SUPABASE_JWT_SECRET=your-jwt-secret

# ============================================
# Cron Jobs (Tùy chọn - cho Vercel Cron)
# ============================================
# Tạo secret ngẫu nhiên: openssl rand -base64 32
# CRON_SECRET=your-random-secret

# ============================================
# Analytics (Tùy chọn)
# ============================================
# Vercel Analytics tự động bật trên Vercel deployments
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your-analytics-id

# ============================================
# Chỉ dùng cho Development
# ============================================
# Đặt "true" để bật logging chi tiết
# DEBUG=false
